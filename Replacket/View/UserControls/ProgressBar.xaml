<UserControl x:Class="Replacket.View.UserControls.ProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Replacket.View.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">

    <Grid Background="{StaticResource MaterialDesignLightSeparatorBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
            Style="{StaticResource ProgressHeadline}" 
            VerticalAlignment="Center" />

        <ProgressBar Grid.Row="1" 
             x:Name="progressBar"
             Style="{StaticResource MaterialDesignLinearProgressBar}" 
             Value="{Binding Progress}"
             Height="30" 
             Margin="15,0,15,20"
             BorderBrush="{StaticResource MaterialDesignShadowBrush}"
             BorderThickness="2"
             Background="#B0B0B0"
             Foreground="#4CAF50"/>

        <TextBlock Grid.Row="1"
               Text="{Binding Value, ElementName=progressBar, StringFormat={}{0:0}%}" 
               HorizontalAlignment="Center" 
               VerticalAlignment="Center" 
               FontSize="20"
               FontWeight="SemiBold"
               Foreground="{StaticResource PrimaryTxtColorBrush}"
               Margin="15,0,15,20"/>

    </Grid>
    
</UserControl>
