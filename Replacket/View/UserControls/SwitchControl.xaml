<UserControl x:Class="Replacket.View.UserControls.SwitchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Replacket.View.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">

    <Grid Background="{StaticResource MaterialDesignLightSeparatorBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!-- Start btn -->
        <Button Grid.Column="0" Style="{StaticResource StartBtnStyle}" />

        <!-- Stop btn -->
        <Button Grid.Column="1" Style="{StaticResource StopBtnStyle}" />

    </Grid>
    
</UserControl>
