<Window x:Class="Replacket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Replacket"
        xmlns:userControls="clr-namespace:Replacket.View.UserControls"
        mc:Ignorable="d"
        Title="Replacket" Height="600" Width="900"
        ResizeMode="NoResize">
    
    <!-- Main Grid -->
    <Grid Background="{StaticResource MaterialDesignShadowBrush}">
        <Grid.RowDefinitions>
                <RowDefinition Height="9*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Packet info -->
            <userControls:PacketInfoControl DataContext="{Binding PacketVM}" Grid.Row="0" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Progress bar -->
            <userControls:ProgressBar Grid.Row="1" />

        </Grid>

        <!-- Right side Grid -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="9*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <!-- Settings tab -->
            <userControls:SettingsControl Grid.Row="0" />

            <!-- Switch control -->
            <userControls:SwitchControl Grid.Row="1" />
            
        </Grid>
    </Grid>
    
</Window>
