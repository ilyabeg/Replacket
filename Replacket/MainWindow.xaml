<Window x:Class="Replacket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Replacket"
        xmlns:userControls="clr-namespace:Replacket.View.UserControls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Replacket" Height="600" Width="900"
        ResizeMode="NoResize">

    <materialDesign:DrawerHost x:Name="drawer" OverlayBackground="Black">
        <materialDesign:DrawerHost.RightDrawerContent>
            <Grid Width="Auto" Background="{StaticResource MaterialDesignDarkSeparatorBackground}">

                <!-- Settings control -->
                <userControls:SettingsControl/>

            </Grid>
        </materialDesign:DrawerHost.RightDrawerContent>

        <!-- Content wrapper grid -->
        <Grid>

            <!-- Main Grid -->
            <Grid Background="{StaticResource MaterialDesignShadowBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="9*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Packet info -->
                <userControls:PacketInfoControl DataContext="{Binding PacketVM}" Grid.Row="0" />

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6*"/>
                        <ColumnDefinition Width="4*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Progress bar -->
                    <userControls:ProgressBar Grid.Column="0" />

                    <!-- Switch control -->
                    <userControls:SwitchControl Grid.Column="1" />

                </Grid>
            </Grid>

            <Border Background="#363738" 
                Visibility="{Binding IsRightDrawerOpen, ElementName=drawer, Converter={StaticResource BooleanToVisibilityConverter}}" />
            
            <Button Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
                CommandParameter="{x:Static Dock.Right}"
                Style="{StaticResource OpenSettingsBtn}"/>

        </Grid>
    </materialDesign:DrawerHost>
</Window>
